import 'package:flutter/material.dart';
import 'package:olly_el3zm/features/settings/presentation/views/widgets/settaings_body.dart';

class SettaingsScreen extends StatelessWidget {
  const SettaingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SettaingsBody(),
    );
  }
}
